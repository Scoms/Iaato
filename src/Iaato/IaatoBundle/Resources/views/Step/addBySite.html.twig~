{% extends '::base.html.twig' %}

{% block title %}
	Expedition Leader
{% endblock %}

{% block body %}

  
<ul class="nav" style="margin-top:15px;">
	<a class="btn btn-info" href="{{ path('iaato_step_list',{"index":1) }}" >Show steps</a>
	<a class="btn btn-info" href="{{ path('iaato_step_list_add',{"param":"null"}) }}" >Add a step</a>	
	<a class="btn btn-info" href="{{ path('iaato_step_list_remove') }}" >Remove a step</a>
</ul>
<h2>Step add</h2>
<h5>Booking of {{ site }}</h5	>

{% if disponibilities is empty %}
<p> is free the whole month.</p>
{% else %}
<table class="table table-hover table-bordered" style="background-color:white;">
    
    <thead style="background-color:#49afcd">
      <tr>
          <th style="width:150px;color:iophyjkb">Time Slot</th>
          <th style="color:iophyjkb">By</th>
          <th style="color:iophyjkb">Phones</th>
          <th style="color:iophyjkb">Emails</th>
      </tr>
    </thead>
    {% for dispo in disponibilities %}
    <tr>
      <th>{{ dispo.timeslot }}</th>
      <th>{{ dispo.ship }}</th>
      <th>
      {% for phone in dispo.ship.phone %}
	{{ phone }}
      {% endfor %}
      </th><th>
      {% for email in dispo.ship.email %}
	{{ email }}
      {% endfor %}
      </th>
    <tr>
    {% endfor %}
</table>
{% endif %}
</div>
{% endblock %}
